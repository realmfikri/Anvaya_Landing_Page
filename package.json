{
  "name": "anvaya-landing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "preview": "npx wrangler pages dev dist",
    "deploy:cloudflare": "npm run build && npx wrangler pages deploy dist --project-name anvaya-landing-page --branch main"
  }
}
