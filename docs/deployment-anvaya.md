# Anvaya Deployment

The active deployment target is Cloudflare Pages for:

```text
https://anvaya.co.id
https://www.anvaya.co.id
```

The previous GitHub Pages deployment notes are superseded. Use the current Cloudflare runbook:

```text
docs/deployment-cloudflare-anvaya-co-id.md
```
